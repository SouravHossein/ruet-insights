
The user has multiple issues + new feature requests. Let me break it down:

1. **Build errors** - 2 TS errors blocking build
2. **OAuth error** - "missing OAuth secret" - means Google provider isn't actually enabled on the backend even though the configure tool was called
3. **Phone auth** - need to implement properly with OTP flow
4. **Mobile-first UI** - current modal/layout needs redesign
5. **Gamification** - burning fire animation on district selection
6. **Less clutter** - strip text guidance
7. **Validation timing** - only show name/dept/merit mismatch AFTER submit, not during search

Let me check current state of key files quickly to design the right plan.

Plan scope is clear enough. Keep concise.
